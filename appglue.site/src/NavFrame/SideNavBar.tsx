import {FrameOptions} from "./FrameOptions";


export function SideBarNav(props: {layoutOptions: FrameOptions}) {
    // render pages
    //  - category (w/icon)
    //    - sub categories (if exist)
    //    - page name
    return (
        <div>portal</div>
    );
}