import React from "react";
import {FrameOptions} from "./FrameOptions";

export function TopBarNav(props: {layoutOptions: FrameOptions}) {
    // render pages
    //  - category (w/icon) dropdown
    //    - sub categories (if exist)
    //    - page name
    return (
        <div>portal</div>
    ); 
}   